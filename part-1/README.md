# FTP

> Part 1. Accessing files

Create a script `part-1.sh` which downloads a bash script from the server and runs it.

The server is running on `server`. You can connect to it via the FTP protocol.

The file you need is accessible via the endpoint `java/kotlin/groovy.sh`.

Credentials are passed via env variables: `USER` & `PASSWORD`.

> _Your script must not pollute the filesystem!_

## Error handling

Is not needed. We guarantee the correctness of the input data.

---

* Which port does FTP use?
* Recall the docker installation guide: how does it work?
